{
  "name": "planeacion-functions",
  "description": "Cloud Functions proxy para la API de Anthropic",
  "engines": { "node": "22" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:emu": "firebase emulators:exec --only firestore --project demo-planea 'vitest run'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.3",
    "openai": "^4.73.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0",
    "firebase-functions-test": "^3.4.0",
    "vitest": "^2.1.9"
  },
  "private": true
}
